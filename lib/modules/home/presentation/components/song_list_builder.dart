import 'package:flutter/material.dart';

class SongListBuilder extends StatelessWidget {
  const SongListBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Song list builder here"));
  }
}
